<!DOCTYPE html>
<html lang="en">
    <head>
        <meta charset="utf-8">
        <meta name="viewport" content="width=device-width, initial-scale=1">
        <title>inventorys</title>
        <link rel="stylesheet" href="https://maxcdn.bootstrapcdn.com/bootstrap/3.4.1/css/bootstrap.min.css">
        <script src="https://ajax.googleapis.com/ajax/libs/jquery/3.5.1/jquery.min.js"></script>
        <script src="https://maxcdn.bootstrapcdn.com/bootstrap/3.4.1/js/bootstrap.min.js"></script>
    </head>
    <body>
        <div class="jumbotron text-center">
            <h1>Hello! user: <%= name %></h1>
           <p>Showing all <%= ninventory %> Books.</p>
        </div>
        <div class="container-fluid">
            <div class="row ml-md-auto">
                <a href="/logout" class="btn btn-primary pull-right" role="button">Log out :)</a>
                <a href="/create" class="btn btn-primary" role="button">Create new Books Data</a>
            </div>
            <br>
            <div class="row">
                <p>Search Books name:
                <label for="bar"><span class="glyphicon glyphicon-search"></span></label><input style="width:100%" type="text" id="myInput" onkeyup="myFunction()" placeholder="Search name:" name="bar"><br></p>
                <table id="myTable" class="table table-striped table-bordered table-hover table-scroll">
                    <thead ><tr class="table-primary"><th>Books Name</th></tr></thead>
                    <tbody style='height:300px;display:block;overflow:scroll'>
                        <% for (var r of Library) {%>
                            <tr>
                                <td><a href="/details?_id=<%= r._id %>"><%= r.name %></a></td>
                                <td style="display:none;"><%= r.inv_type%></td>
                                <td style="display:none;"><%= r.quantity%></td>
                            </tr>
                        <% } %>
                    </tbody>
                </table>
            </div>
            <p>This is the end of the page ;)</p>
        </div>
        <script>
        $(document).ready(function(){
            $("#myInput").on("keyup", function() {
                var value = $(this).val().toLowerCase();
                $("#myTable tr").filter(function() {
                    $(this).toggle($(this).text().toLowerCase().indexOf(value) > -1)
                });
            });
        });

        </script>
    </body>
</html>
